import LoadingComponent from '@/app/_components/common/LoadingComponent';

export default function Loading() {
  return (
    <div className="loading page-container--full padded center">
      <LoadingComponent />
    </div>
  );
}
